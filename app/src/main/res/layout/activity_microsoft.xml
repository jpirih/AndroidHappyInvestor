<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.kekec_apps.android.happyinvestor.MicrosoftActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/microsoftTitle"
        android:layout_centerHorizontal="true"
        android:text="Microsoft Stock data"
        android:textSize="32dp"
        android:textColor="#000000"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/msBackBtn"
        android:layout_below="@+id/microsoftTitle"
        android:layout_marginTop="20dp"
        android:background="#FF0D00"
        android:text="Nazaj"
        android:textColor="#FFFFFF"/>

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/listMicrosoftStocks"
        android:layout_below="@+id/msBackBtn"
        android:layout_marginTop="20dp"/>

</RelativeLayout>
